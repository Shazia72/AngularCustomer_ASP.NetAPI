import { FindBalancePipe } from './find-balance.pipe';

describe('FindBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new FindBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
